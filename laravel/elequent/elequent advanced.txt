................................................course index 
1...stub:publish
2.. upsert
3.. updateOrCreate
3.1.. firstOrCreate
4.. isDirty
4.1.. wasChanged
4.2.. wasRecentlyCreated 
5.. opserver
6..accessors
6.1..mutator
7..touch()
8..saveQuietly : to save without run opserver or listener
8.1..updateQuietly : to update without run opserver or listener
9..all(['id','andAnother'])
9.1..findOrFail
9.2..find spasific column
9.4..firstOrFail
9.5..firstOr
9.6..findOr
10..compare between dateTime with date
10.1..whereDate
10.2..whereYear
10.3..whereMonth
10.4..whereDay
11.1..toSql()
11.2..if you want use where and orWhere but you have a where you want not ignor it if or is true
12..if you want repeating quiry you can use gloable scopes and local scops
12.3..gloable scope
12.3.2.2..if you want call with this scop call model only like this
13..laravel when
14..selectRow
14.1..whereRaw:if you want use MONTH in created_at
15..one to many ploymorphic
15.1..spaty laravel media libaray: package to manage photos
16..ploymorphic many to many // in model that morph as photo return $this->morphedByMany in another model $this->morphToMany(Photo::class, 'photable');
17..belongsToMany:use it if you have the pivoteTable for many to many relation
18..hasManyThroph: if you want make nested relation in model
19..has('relatioName'),whereHas(),withWhereHas
19.1..search whereNot(),whereBelongsTo(),whereRelation()inmodel instead of whereHas inquery,
20..latestOfMany()
20.1..oldestProject()
20.2..ofMany('id', 'max')
20.4..i can put condition in orderby
20.5..add acondition in ordrBy and where
21..if i want use spatie package
22..package if you want make search in morethan one model 
22.1..package1 from spate called laravel searchable
22.2..package2 called laravel cross elequent search
23..package called elequent has many deep : is package for deep nested realtin in model
24..package called parental : to make speret model extend from one model like model user can be in model admin and model manager each model has one table is user
.................................................................................................
https://laraveldaily.com/lesson/eloquent-the-expert-level/customize-model-template-stubs
...video
1..how to chang model structure creating 
1.1..php artisan stub:publish
1.2..will creat folder that have standard
1.3..go to stubsfolder\model_stub
change any thing
4..to update data from excel
4.1..$modelNAme::upsert([search => value of search,columnName => value you want update],
[column you want updated for it]
);
5..firstOrCreate
6..updateOrCreate
.................................
https://laraveldaily.com/lesson/eloquent-the-expert-level/wascreated-isdirty-model-changed
..video link
1.. if you want check the request update in data before saving update or not use isDirty function in opject
$user->name = 'any think to update for it';
$user->isDirty('name')? updated:not updated;
$user->save;
1.1..if you want check data saved in DB or not use $opject->wasChanged('coulmnYouWant');
$user->wasChanged('any column')? 'changed' : 'not changed';
2..if you want check the data created or not
$user=Model::create([any thing]);
$user->wasRecentlyCreated ? 'created' : 'not Created';
............................video
https://laraveldaily.com/lesson/eloquent-the-expert-level/model-observers
....
5..opserver class: the class that for spasifec model you can write in it the action you want done after
use this model in create or update
5.1..create observer for spasific model
php artisan make:observer ModelNAmeObserver --model=ModelNAme
5.2..define the observer in eventServicesProvider in tow way
5.2.1.. got to eventServicesPronider->boot
ModelName::observe(ObserbeNAme::class);
5.2.2..or you can do this in eventServicesPronider
protected $observers=[
	ModelName::class => ObserverNAme::clas
];
5.3.. if you want make code execute in DB after run create or update
make the function in observer class called
creating or updating
..................................................VIDEO.................................
https://laraveldaily.com/lesson/eloquent-the-expert-level/attributes-accessors-mutators
6..accessors and mutators ..poth called attrbute in laravel 9
6.1..accessors : used if you want change the value when getting the data of setting the data
6.1.1..muttator : used if you want change the value when saving the data of setting the data
6.2.. go to model to create accessor to can access it after caled it
public function createdDiff(): Attrbute //name of function must be functionNameDiff,:Attribute is the return of function
 {
	return Attribute::make( //must be define Attribute 
	get: fn() => $this->created_at->diffForHumans();//this function to get the time that this filed created_at untile now like created from 5 mint
           //diffForHumans() : carpon function to calculate the time from creaqted_at to now
);

}
6.2.1..to get this accestor data can you call it by
$user->created_diff; // created:the first in function name that name is createdDiff() , _diff:is the second  
6.3..to create mutator funtion to make change of data befor save it
public function name(): Attrbute // name():must be column Name you want changes it 
{
	return Attrbute::make(
	set: fn($value) => ucfirst($value) //here the $value:is function name taht mean the columnName
	//ucfirst($value): function to make the first char is capital
	);
}

6.3.1..you can use  mutator to stoped exception of date error formate by
public function birthDate(): Attrbute //the coulmn name birth_date = in model birthDate
{
  return Attrbute::make(
	get: fn($value)=> Carbon::createFromFormate('Y-m-d',$value)->format('m/d/Y'), //for get th date as user enter it
	set: fn($value) => Carbon::createFromFormat('m/d/Y',$value)->format('Y-m-d') //for save  the date as DB formate
);
} 
.....................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/touch-method-updated-at
...
7.1..if you want update the updatd_at to be now you can use touch() like this
modelName::where(any thing)->touch();//will updated updated_at in all rows athe time now
7.2..if i want if i change updated_at in a chiled taple the updated_at in perant table to be update
7.2.1..go to chiled and add
protected $touchs = ['nameOfRelation'];
7.2.2..if i want the updated _at not changed you can use this in controller befor update
$opjectName->timestamps = false;
..................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/find-all-first
9.1..you can use all() and define the coulmn like that
all(['id','andAnother'])
9.2..find to find data by id and select column
find(idNumber,['any column','another']);
9.3..find alote of one id 
find([1,2]);
9.4..to check if this id find return data else return exception
findOrFail(IdNumber);
9.5..if you want define the return if fail
firstOr(function(){abort(403)}); //if fail will return 403
9.6..findOr(idNumber,function()=> abort(403));
.......................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/wheredate
10..if you want compare between dateTime with date you can use
10.1.1..
where(DB::row('DATE(created_at)') , 'yourDate')
10.1.2..
whereDate('created_at' , 'yourDate');
.......................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/brackets-and-or-conditions
11.1..if you get statement of sql query you can use toSql()
modelNAme::where(any)->toSql();
11.2..if you want use where and orWhere but you have a where you want not ignor it if or is true
modelNAme::whereNotNull('anyColumn')
->where(function($q){
$q->where()->orWhere();
})->get();
.................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/local-global-scopes
12...if you want repeating quiry you can use gloable scopes and local scops
12.2.1..first will use local scop  
12.2..you can do this by put query in function into model
12.2.1..go to model and write
public function scopeFunctionName($query)  //function name must be start by scope and first char after this must be capital 
{
return $query->where('is_completed' , true);
}
12.2.1.1..if you want use this scope can you it like this
ModelName::scopeName()->get(); //the scope name must be the same but wihout scope and the firest char convert it to lower case
12.2.2..you can write function in model that take a pramter like this
12.2.2.1..go to model and write
public function scopeName($query,$anyVariaple)
{
	$query->where('is_completed' , $anyVariaple);
}
.....
12.3..gloable scope automatically globally on all the models
12.3.1..to make function in model scope you can define it in public static function booted()
12.3.1.1..you have tow way 1 static 2 dynamic
12.3.1.2..you can call function that it in model like this
public function booted()
{
static::addGlbalScope(new NameOfGloableScopeModelClassName());//class name that created here 12.3.1.3
} 
12.3.1.3..create model called nameScope and make this
class Name implements Scope // and defin scope

public function apply(Builder $builder, Model $model)
{
 	$builder->where();
}
her when call model the condetion will run automatic like this
ModelName::all();
12.3.2..another way for gloable query
12.3.2.1..go to model and add this function
public static function booted()
{
static::addGlobalScope('scopeName',function(Builder){
$builder->where('column',value);
});
}
12.3.2.2..if you want call with this scop call model only like this
ModelName::all();
12.3.2.3..if you want ignor or stop all global query use it
modelName::withoutGlobalScope()->get();
12.3.2.3..if you want ignor or stop spacific global query use it
modelName::withoutGlobalScope(['scopeName'])->get();
...........................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/eloquent-when
13..use laravel when
modelName::query()->when($requent->variableYouWantCheckIfIsExistDoThisFunction, function($q){
$q->where();
} )->get();
..............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/raw-queries
14..if you want use DB row you can use selectRow instead it like
ModellName::selectRaw('id,MONTH(created_at) as created_month')->get(); //month to select month only from created_at
14.1..whereRaw:if you want use MONTH in created_at
whereRaw('MONTH(created_at) = 10')
14.2..you can use it but not good becouse laravel not protected the variaple in row
whereRaw('MONTH(created_at) = ?' request('month'))
............................................................VIDEO Advanced Eloquent Relations
https://laraveldaily.com/lesson/eloquent-the-expert-level/polymorphic-relations-example
15..one to many ploymorphic polymorphic NOT HAVE RELATION BY DB 
15.1..i need in polimorphic table are
1..id :of table 
2..type : of model thate whte will belong to
3..id of model : unsignedBigInteger
4.. 2and3 you can make it in one like this
$table->morph(modelNam);
5..go to model that it morph and make this

public function functionName()
{
return $this->morphTo();
}
//in this model you not need define any of belongTo or hasMany
6..in another model that have relation many you can define it like
public function  functionName()
{
return $this->morphMany(ModelName::class,functioNAmeInRelational);
}
..............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/polymorphic-many-to-many
16..ploymorphic many to many 
16.1..we have photo table that all table relation with it
16.2..will create pivote table called photable
$table->forignId('photo_id')->constrained();
  $table->unsignedBigInteger('photable_id'); // this id of table of photable_type
$table->string('photable_type'); // the model 
16.3..in model of photo
public function user()
{
	return $this->morphedByMany(User::class, 'photable') //photable the name of pivote TAble or relation
}
16.4..in another model 
public function photo()
{
return $this->morphToMany(Photo::class, 'photable');
}
..................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/advanced-belongstomany-pivot-table
17..belongsToMany:use it if you have the pivoteTable for many to many relation
17.1..pivote table has forignkey only if you add any column you can define it in relation like

$this-.belogsToMany(model::class)->withPivote('columnName')->withTimeStamps();

17.2..i can use where in model for pivote table by use wherePivote()
17.2.1..if i can use pivote in model put execute if i call not pivote put by another name 
$this->belongsToMany(model::class)->as('any name i can call by it instead of pivote')
->wherePivote()
17.3..to call coulmn inside pivotetable must be get foreach of relation and $relation->pivote->column or $relation->the name in as->column
.................................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/hasmanythrough-2-level-hasmany
18..hasManyThroph: if you want make nested relation in model 
18.1..go to model and make relation like this
public function tasks()
{
return $this->hasManyTrouph(tasks::class, Project::class);
}
......................................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/query-relationship-data
19..has('relatioName'): use it to get data that has thes relation only
19.1..has('relation', '>', 1): get data that have from this relation more than one
19.2..whereHas('relation',function): get data that has relation only and this relation have some of condition and get only the relation that have this condition
19.3..withWhereHas('relation',function) as the same in 19.2 
............................................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/withcount
20..withCount('relationName'): if you want get the relationData and the count of this relation
20.1..if i get count 
$opject->relatioName_count
20.2..to get tha max id of the relation
withMax('relation', 'id')
20.2.1..to get max id
$opject->relationName_max_id
20.3..to get min of id
withMin('relationName', 'id')
20.3.1..to get min id
$opject->relationName_min_id

20.4..to get avarage of id
withAvg('relationName', 'id')
20.4.1..to get avarage id
$opject->relationName_avg_id

20.5..to check exist or not
withExist('relationName')
20.5.1..to get 
$opject->relationName_exist
.........................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/ofmany-latestofmany-oldestofmany
20..if i have hasMany relationship but i want latest only i can do this by ; all in this episot if you want get one record from relationship
public function project()
{
return $this=->hasOne(Projrct::class);
}
public  function latestProject()
{
 return $this->hasOne(project::class)->latestOfMany();
}
20.1..the opesit of latest is oldestOfmany() to get first in model
public function oldestProject(){
	return $this->hasOne(Project::class)->oldestOfMany();
}
20.2..if you want add column to order by in model you can use ofMany('id', 'max')
public function someOrderProject()
{
	return$this->hasOne(Project::class)->ofMany('id', 'max'); // order by max of id
}
20.3..if you want add column to order by in model you can use ofMany('id', 'max')
public function someOrderProject()
{
	return$this->hasOne(Project::class)->ofMany('id', 'min'); // order by minmum of id
}
20.4..i can put condition in orderby like if max id is the same orderby another column like max createdby
public function someConditionProject()
{
	return $THIS->hasOne(Project::class)->ofMany(['id' => 'max', 'updated_at' => 'max'])
} 
20.5..add acondition in ordrBy and where
return $this->hasOne(Project::class)->ofMany(['id' => 'max', 'creted_at' => 'max'], function($q){
	$q->where('name', 'like', '%somthing%');
});
**1..if i want get opject from model to query
$query=Model::query();
...............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/spatie-query-builder-eloquent-filter
21..if i want use spatie package 
21.1..spatie filter
..............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/spatie-laravel-searchable-cross-eloquent-search
22..package if you want make search in morethan one model 
22.1..package1 from spate called laravel searchable
22.2..package2 called laravel cross elequent search
.....................................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/hasmanydeep
23..package called elequent has many deep : is package for deep nested realtin in model
............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/parental-tighten-relationship-self
24..package called parental : to make speret model extend from one model like model user can be in model admin and model manager each model has one table is user
............................................................VIDEO
https://laraveldaily.com/lesson/eloquent-the-expert-level/cascade-soft-deletes-children
25..package cascading soft deletes : 